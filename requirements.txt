# --- Core Web ---
fastapi
uvicorn[standard]
python-multipart
aiofiles

# --- Config & Environment ---
pydantic-settings
python-dotenv

# --- Database ---
motor

# --- Auth & Security ---
passlib[bcrypt]
email-validator
python-jose[cryptography]
bcrypt==4.0.1

# --- RAG Engine (Free / Local) ---
faiss-cpu==1.12.0
sentence-transformers>=2.6.1
langchain==0.3.25
langchain-community==0.3.24

# --- Optional (future switch or embedding alternatives) ---
# chromadb              # keep commented unless you need remote Chroma
# google-genai          # paid, disable for free mode

# --- Document Loading & Parsing (Light but Complete) ---
PyMuPDF
python-docx
python-pptx
openpyxl
xlrd
pandas
pillow                # for image OCR if you ever re-enable pytesseract
pdfplumber            # still lightweight

# --- Optional OCR (disabled for speed) ---
# pytesseract
# unstructured==0.17.2
# unstructured[all-docs]

# --- ML / AI Utilities ---
torch>=2.3.0          # compatible with sentence-transformers CPU
transformers
einops
numpy

# --- New Additions (needed for new free RAG pipeline) ---
requests              # for local LLM (Ollama) HTTP call
tqdm                  # progress bar (optional, keep)
